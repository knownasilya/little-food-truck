# For the desktop dev build and the iOS simulator, localhost reaches your
# machine directly. For the Android emulator, use http://10.0.2.2:8787
# instead. For a physical device, use your machine's LAN IP.
VITE_API_URL=http://localhost:8787
